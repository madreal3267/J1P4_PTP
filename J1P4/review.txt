review.txt

	0609

	1.  각 VO마다 테이블을 1개씩 작성하는 것으로 생각, 테이블의 각 컬럼을 필드로 가지고, 도메인 모델 구성
   		DTO 특정 기능 수행하기 위한 필요한 데이터 조합
   
	   -> 두 개의 테이블의 조인 값을 어떻게 처리할 지 고민하였다.
	      1개의 테이블에 1개의 VO 객체를 작성한다고 생각했음
	      특정 기능 (하나의 테이블의 값만 사용하는 것이 아닌 조인과 같은 것들)을 수행하기 위해
	      DTO 객체를 별도로 작성하였음.
	      
	2. AdminController 에서 회원 정보 관리(전체, 프리랜서, 클라이언트)에 3개의 컨트롤러를 사용해야한다고
	   생각하였음 -> 파라미터를 받아서 한개의 컨트롤러 에서 조건에 따라 서비스객체를 받아오도록 작성
	   장단점이 있다고 생각함. 하나의 컨트롤러에서 처리 시 
	   코드 간결성, 유지 보수 용이, 단일 진입점의 장점 정도가 있지만 복잡성이 증가되고,
	   단일 원칙 책임 위반 가능성이 생길 수 있기 때문에 상황에 따라 사용해야 한다고 생각했음.
	   
	3. 전체 테이블 reg_date(최신순정렬)기준으로 order by 절을 사용하였는데 mysql에서 개별 select 문에 대한
	   order by 절을 지원하지 않아 서브쿼리로 union 결과를 먼저 생성 한 후 정렬하였음.
	   
	4. DataTables 클래스 사용 난감...
	
	